let names = prompt("Ismingizni kiriting...");
let id = prompt("IDni kiriting..");
let password = prompt("Passwordni kiriting..");


const marsit = {
    name : names,
    spaceID : id,
    password : password,
};

console.log(marsit);
